var roadTrafficSignal = prompt("Enter the traffic signal color (Red, Yellow, Green):");

if (roadTrafficSignal == "Red") {
    alert("Must Stop");
} else if (roadTrafficSignal == "Yellow") {
    alert("Ready to move");
} else if (roadTrafficSignal == "Green") {
    alert("Move now");
}