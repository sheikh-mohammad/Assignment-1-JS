var a = 4;
if (++a === 5) {
    alert("given condition for variable a is true");
}

// alert("given condition for variable a is true");
// This script runs because ++a is pre-increment and pre-increment means first update and then use it as well.
